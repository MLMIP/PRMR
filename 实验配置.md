# 环境配置指南 (Environment Setup Guide)

本指南将帮助您从零开始配置项目所需的深度学习环境。本项目基于 **Python 3.8**、**PyTorch 1.12.1 (CUDA 11.6)** 以及 **MMCV-Full 1.7.1** 构建。

---

## 1. 前提条件 (Prerequisites)

在开始之前，请确保您的系统已安装：
* **NVIDIA 显卡驱动**（建议支持 CUDA 11.6 或更高版本，且在 A100 上运行）
* **Anaconda** 或 **Miniconda**

---

## 2. 分步安装步骤 (Step-by-Step Installation)

为了避免依赖冲突（尤其是 `mmcv-full` 的编译问题），请**严格按照以下顺序**进行安装：

```bash
### 步骤 A：创建并激活 Conda 环境
建议使用 **Python 3.8**，它与该套件中的 `numpy (1.21.6)` 和 `matplotlib (3.5.3)` 等基础库有极佳的兼容性。

# 创建新环境
conda create -n prmr python=3.8 -y

# 激活环境
conda activate prmr

### 步骤 C：安装 MMCV-Full (核心步骤)mmcv-full==1.7.1 需要与 PyTorch 和 CUDA 版本严格对应 。请勿直接使用 pip 编译，直接下载官方预编译好的 Wheel 文件最为稳妥：  


pip install mmcv-full==1.7.1 -f https://download.openmmlab.com/mmcv/dist/cu116/torch1.12.0/index.html


### 步骤 D：安装其余依赖项完成上述核心核心硬核库的安装后，即可一键安装剩下的常用工具包（如 timm、opencv、wandb 等） ：  


pip install -r requirements.txt

```

## 3. 实验步骤

首先确保"PRMR\local_configs\_base_\datasets\acdc_1024x1024_repeat.py"文件中的数据集路径是正确的：
```
data_root = 'dataset/ACDC'
```

然后在终端运行下面命令即可：
```
python3 tools/prmr.py
```